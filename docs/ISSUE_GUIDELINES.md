# Правила оформления issue

Этот документ определяет единый формат backlog и задач в репозитории. Он является
источником истины для Epic и Task, структуры issue, размеров, зависимостей,
критериев готовности и меток. Инженерные правила разработки остаются в
[`AGENTS.md`](../AGENTS.md).

## Язык

Issue оформляются на русском языке.

Названия файлов, команды, API-маршруты, переменные и другие технические
идентификаторы указываются в обратных кавычках: `pnpm check`,
`POST /api/generate`, `LlmGateway`.

## Модель backlog

Backlog строится по схеме `Epic → Task`, когда одна конечная инициатива требует
нескольких самостоятельных задач и PR.

Epic не обязателен для каждой задачи. Одиночная maintenance-, bug-, research- или
feature-задача может существовать без Epic, если у неё один самостоятельный
проверяемый результат. Не создавайте искусственный Epic только ради иерархии или
из-за большого размера одной issue.

Перед созданием новой Task проверьте существующие активные Epic. Если задача
является частью их конечного результата, используйте подходящий Epic вместо
создания параллельной инициативы.

### Epic

Epic описывает конечный самостоятельный результат инициативы, а не постоянную
техническую категорию.

Формат заголовка:

```text
[EPIC] <CODE> — <результат>
```

`CODE`:

- содержит 3–8 понятных латинских букв в верхнем регистре
- отражает смысл инициативы
- уникален среди Epic проекта
- после создания не меняется, даже если название Epic уточняется

Примеры из текущего backlog:

- `[EPIC] AUDIT — Реализовать наблюдаемость и аудит LLM-генераций`
- `[EPIC] AGENTS — Упорядочить инструкции и agent workflows проекта`

Не создавайте бессрочные Epic вроде `CI`, `BUGS`, `BACKEND` или `MISC`. Epic
должен иметь состояние, в котором конечный результат можно проверить и закрыть.

Epic не получает размер `S/M/L`: размер описывает Task, а не инициативу.

Epic закрывается после достижения заявленного конечного результата и завершения
необходимых дочерних задач. Само закрытие всех Task не заменяет проверку
результата Epic.

### Task

Task — конкретное ограниченное изменение с одним законченным и проверяемым
результатом.

Если Task относится к Epic, формат заголовка:

```text
<CODE>-<GitHub issue number> [S|M|L] <действие>
```

Примеры:

- `AUDIT-19 [M] Добавить PostgreSQL и миграции для аудита LLM-генераций`
- `AGENTS-75 [M] Разделить инструкции агентов по областям проекта`

Номер после `CODE` всегда равен настоящему номеру GitHub Issue. Отдельный счётчик
задач не ведётся. Поскольку номер становится известен только после создания
issue, при необходимости сначала создайте Task, получите её номер и сразу
приведите заголовок к каноническому формату.

Если Task не относится к Epic, формат заголовка остаётся:

```text
[S|M|L] <действие>
```

Например:

- `[S] Добавить кнопку копирования заявки`
- `[M] Подготовить MVP к проверке`

Связывайте Task с Epic через GitHub Sub-issues / parent issue. Если доступный
инструмент не поддерживает нативную связь, явно укажите родительский Epic в Task
и Task в Epic ссылкой или чеклистом и сообщите об ограничении.

Существующие issue не нужно массово переименовывать только ради нормализации.
Канонический формат обязателен для новых Task и для существующей issue, если её
роль в Epic существенно пересматривается.

## Заголовок Task

После размера используется глагол в неопределённой форме. Заголовок должен
описывать ожидаемый результат, а не процесс работы.

Хорошие варианты:

- `[S] Добавить кнопку копирования заявки`
- `AUDIT-19 [M] Добавить PostgreSQL и миграции для аудита LLM-генераций`
- `REPEAT-175 [L] Спроектировать профиль повторного обращения в управляющую организацию`

Нежелательные варианты:

- `Поработать над UI`
- `Разные исправления`
- `Frontend`
- `Рефакторинг`

## Размер задачи

Размер показывает объём и связанность изменений, но не приоритет.

### `[S]`

Один локальный и однозначный результат. Изменения затрагивают небольшую часть
одного слоя и могут быть проверены независимо.

### `[M]`

Несколько связанных изменений внутри одного сценария или подсистемы. Может
потребоваться согласование нескольких файлов, тестов и документации.

### `[L]`

Сквозное или архитектурно значимое изменение с несколькими группами работ и
повышенным риском.

Задачу размера `[L]` следует проверить на возможность декомпозиции. Она остаётся
одной задачей только тогда, когда разделение создаст искусственные зависимости
или не позволит получить законченный результат.

## Одна задача — один результат

Issue должна приводить к одному законченному и проверяемому результату.

Связанные мелкие изменения можно объединять, если они относятся к одному
пользовательскому или техническому сценарию.

Не следует объединять в одну задачу работы, которые:

- имеют разные цели
- могут выпускаться независимо
- требуют разных исполнителей и почти не пересекаются по коду
- имеют разный порядок выполнения
- не могут быть проверены общими критериями готовности

## Обязательные разделы Task

### `## Цель`

Коротко объясняет, какой результат должен появиться и зачем он нужен. Цель не
должна быть списком файлов или технических действий.

### `## Входит в задачу`

Содержит конкретный объём работы, ограничения и обязательные проверки. Пункты
должны описывать существенные изменения, а не каждую будущую строку кода.

### `## Критерии готовности`

Содержит наблюдаемые условия, по которым можно однозначно понять, что задача
выполнена. Критерии не должны ограничиваться формулировками «код написан», «всё
работает» или «UI улучшен».

### `## Зависимости и параллельность`

Указывает:

- от каких issue или PR зависит задача
- что она блокирует
- какую часть можно выполнять параллельно
- где ожидается пересечение файлов или конфликт веток
- в каком порядке выполнять зависимые части

Если зависимостей нет, это указывается прямо.

Для Task внутри Epic родительская связь хранится прежде всего через GitHub
Sub-issues / parent issue. Текстовая ссылка используется как fallback или
дополнительный контекст, а не как второй независимый источник иерархии.

## Дополнительные разделы

### `## Контекст`

Добавляется, если без него непонятна причина задачи, предыдущее решение или
важное ограничение. Контекст не должен превращаться в длинную историю проекта.

### `## Не входит в задачу`

Добавляется, если существует риск расширения объёма или смешения с соседними
issue. Особенно полезен для архитектурных задач, UI, рефакторинга и интеграций с
внешними сервисами.

### `## Порядок выполнения`

Добавляется, если внутри issue есть этапы, которые нельзя выполнять в
произвольной последовательности.

### `## Проверка`

Можно использовать для нестандартных ручных проверок, команд или сценариев,
которые неочевидны из критериев готовности.

## Зависимости

Зависимости указываются ссылками на конкретные issue или PR:

- `Зависит от issue #5`
- `Финальная проверка выполняется после PR #3`
- `Блокирует браузерную часть issue #6`

Не следует объявлять всю задачу заблокированной, если параллельно можно выполнить
подготовительную часть. Нужно отдельно описывать, что доступно сейчас, а что
требует завершения зависимости.

При ожидаемом пересечении необходимо перечислить основные конфликтующие файлы
или подсистемы.

## Milestones

Epic и Milestone не смешиваются:

- Epic отвечает на вопрос «какую инициативу реализуем»
- Milestone отвечает на вопрос «к какому релизу или сроку относится результат»

Правила:

- при создании или существенном изменении Task проверяйте её принадлежность к
  подходящему активному milestone
- назначайте milestone атомарным Task, необходимым для достижения результата
  вехи
- оставляйте без milestone backlog-задачи, для которых ещё не определён релиз
- не назначайте milestone Epic, если прогресс вехи уже учитывается по её
  дочерним Task
- пересматривайте milestone при изменении scope, зависимостей или состава релиза
- перед закрытием milestone проверяйте, что обязательные Task завершены или явно
  перенесены
- не задавайте milestone искусственный due date без реального обязательства

## Критерии готовности

Каждый критерий должен быть проверяемым через одно или несколько действий:

- автоматический тест
- команда репозитория
- ручной сценарий
- проверка содержимого файла
- просмотр интерфейса
- отсутствие запрещённых данных или зависимостей

Критерии описывают результат, а не выбранный исполнителем способ реализации.

## Технические ограничения

В issue фиксируются только значимые ограничения:

- архитектурные границы
- требования безопасности и приватности
- запрет на новые зависимости
- совместимость API
- обязательные команды проверки
- требования к обработке ошибок
- требования к доступности

Не следует заранее диктовать имена внутренних функций, классов или мелкие детали
реализации, если они не являются частью контракта.

## Тесты и проверки

Для любых изменений кода issue должна требовать тесты изменённой логики либо
явно объяснять, почему автоматический тест неприменим.

Для проекта обязательной финальной командой остаётся:

```bash
pnpm check
```

Ручная проверка не заменяет автоматическую там, где поведение можно надёжно
проверить тестом.

## Документация

Обновление документации включается только тогда, когда задача изменяет:

- публичное или внутреннее поведение системы
- архитектуру
- настройку приложения
- способ запуска
- контракт API
- правила разработки

Не следует добавлять формальное обновление документации в каждую мелкую задачу.

## Приватность

Issue, комментарии, тесты и примеры не должны содержать:

- личную переписку
- реальные адреса
- телефоны
- фамилии
- номера квартир
- API-ключи
- пользовательские тексты, не прошедшие обезличивание

Для примеров используются вымышленные или обезличенные данные.

## Метки

На текущем размере проекта достаточно существующего набора меток. Иерархия
Epic/Task определяется заголовком и parent/sub-issue связью и не требует нового
набора type-меток только ради единообразия.

Тип задачи:

- `enhancement` — новая возможность или улучшение
- `bug` — подтверждённая ошибка
- `documentation` — преимущественно изменение документации

Участие:

- `good first issue` — локальная задача без скрытых архитектурных зависимостей,
  подходящая новому участнику
- `help wanted` — задача, для которой действительно требуется или
  приветствуется отдельный исполнитель

Размер указывается в заголовке, поэтому отдельные метки `size:S`, `size:M` и
`size:L` пока не нужны.

Метки областей вроде `frontend`, `backend` или `llm` стоит вводить только после
появления нескольких одновременно открытых задач в каждой области.

## Приоритет

Размер задачи не означает её приоритет.

Пока backlog небольшой, порядок работы указывается через зависимости и текст
issue. Отдельные метки приоритета следует вводить только тогда, когда открытых
задач станет достаточно много и их порядок перестанет быть очевидным.

## Связь с pull request

PR должен ссылаться на Task через `Closes #N` или `Fixes #N`, если он полностью
завершает задачу.

Одна Task обычно закрывается одним PR. Несколько PR допустимы для большой задачи,
если в issue заранее указан порядок частей и понятно, какой PR закрывает её
окончательно.

Epic обычно не закрывается одним PR: она агрегирует конечный результат нескольких
Task. PR может ссылаться на Epic для контекста, но должен закрывать конкретную
Task, когда такая Task существует.

Решения, меняющие объём задачи, фиксируются не только в комментариях: после
согласования описание issue обновляется и остаётся актуальным источником
требований.
