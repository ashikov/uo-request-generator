# Базовые настройки сервера
# HOST=0.0.0.0
# PORT=3000

# Защита генерации
# Для запуска с подключённым LLM обязателен случайный секрет длиной не менее 32 символов:
# GENERATION_CLIENT_COOKIE_SECRET=replace-with-a-random-secret-at-least-32-characters
# Опциональные лимиты и настройки с текущими значениями по умолчанию:
# GENERATION_IP_REQUEST_LIMIT=3
# GENERATION_IP_WINDOW_MS=60000
# GENERATION_CLIENT_DAILY_LIMIT=20
# false безопасно по умолчанию. true доверяет forwarding-заголовкам от любого
# источника, который может обратиться к backend, и требует изоляции backend за
# доверенным reverse proxy с обязательной перезаписью X-Forwarded-For и X-Forwarded-Proto.
# Ограниченный allowlist доверенных proxy относится к отдельной issue #61.
# GENERATION_TRUST_PROXY=false
# GENERATION_RATE_LIMIT_STATE_CAPACITY=10000

# SmartCaptcha
# Локальный режим без внешнего скрипта и серверной проверки:
SMARTCAPTCHA_MODE=disabled
# Для обязательного режима замените mode и задайте оба обезличенных значения:
# SMARTCAPTCHA_MODE=required
# SMARTCAPTCHA_CLIENT_KEY=replace-with-public-client-key
# SMARTCAPTCHA_SERVER_KEY=replace-with-private-server-key

# Примеры конфигурации:
# Выберите один сценарий и раскомментируйте его обязательные переменные.
# Опциональные переменные включайте только при необходимости.
# Для новых конфигураций указывайте LLM_API_PROTOCOL явно.
# Без него для обратной совместимости используется chat-completions.

# Yandex AI Chat Completions
# Встроенный endpoint: https://ai.api.cloud.yandex.net/v1/chat/completions
# Обязательные переменные:
# LLM_API_PROTOCOL=chat-completions
# LLM_API_KEY=your_yandex_api_key
# LLM_FOLDER_ID=test-folder-id
# Опционально (по умолчанию будет использована модель gpt://<LLM_FOLDER_ID>/yandexgpt/latest):
# LLM_MODEL=gpt://test-folder-id/yandexgpt/latest

# Yandex AI Responses API
# Встроенный endpoint: https://ai.api.cloud.yandex.net/v1/responses
# Обязательные переменные:
# LLM_API_PROTOCOL=responses
# LLM_API_KEY=your_yandex_api_key
# LLM_FOLDER_ID=test-folder-id
# Опционально (по умолчанию будет использована модель gpt://<LLM_FOLDER_ID>/aliceai-llm-flash/latest):
# LLM_MODEL=gpt://test-folder-id/aliceai-llm-flash/latest

# OpenAI-compatible Chat Completions
# Обязательные переменные:
# LLM_API_PROTOCOL=chat-completions
# LLM_API_URL=https://provider.example/v1/chat/completions
# LLM_API_KEY=your_api_key
# LLM_AUTH_SCHEME=Bearer
# LLM_MODEL=provider-model-name

# OpenAI-compatible Responses API
# Обязательные переменные:
# LLM_API_PROTOCOL=responses
# LLM_API_URL=https://provider.example/v1/responses
# LLM_API_KEY=your_api_key
# LLM_AUTH_SCHEME=Bearer
# LLM_MODEL=provider-model-name
